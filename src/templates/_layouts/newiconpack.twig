{% extends "_layouts/cp" %}

{% set fullPageForm = true %}

{% set crumbs = [
    { label: settings.pluginName, url: url("one-plugin-fields") },
    { label: "SVG Icon Packs"|t("one-plugin-fields"), url: url("one-plugin-fields/svg-icons") },
]
%}

{% block actionButton %}
    <div class="buttons">
        <div class="btngroup submit">
            <input type="submit" class="btn submit" value="{{ 'Save'|t('one-plugin-fields') }}">
        </div>
    </div>
{% endblock %}